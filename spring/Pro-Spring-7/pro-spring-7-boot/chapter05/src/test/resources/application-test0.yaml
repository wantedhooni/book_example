spring:
  datasource:
    url: "jdbc:h2:mem:mydb"
    username: "sa"


# Logging config
logging:
  pattern:
    console: "%-5level: %class{0} %blue(%logger{15}) - %msg %n"
  level:
    root: INFO
    # org.springframework.boot: DEBUG
    com.apress.prospring6.six: DEBUG
