# datasource config
spring:
  datasource:
    driverClassName: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/musicdb?useSSL=false
    username: prospring7
    password: prospring7
    # HikariCP
    hikari:
      maximum-pool-size: 25
  # JPA config
  jpa:
    properties:
      hibernate:
        naming:
          physical-strategy:
        jdbc:
          batch_size: 10
          fetch_size: 30
        max_fetch_depth: 3
        show-sql: true
        format-sql: false
        use_sql_comments: false
        hbm2ddl:
          auto: none
        dialect: "org.hibernate.dialect.MariaDBDialect"


# Logging config
logging:
  pattern:
    console: "%-5level: %class{0} - %msg%n"
  level:
    root: INFO
    org.springframework.boot: DEBUG
    com.apress.prospring7.boot.nine: INFO
    org.springframework.orm.jpa: TRACE
